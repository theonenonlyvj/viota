onlyBuiltDependencies[]=better-sqlite3
# Pin Node for pnpm-run scripts: better-sqlite3 has no prebuilt binary for Node 26
# and this machine's C++ toolchain can't compile it (stale Xcode CLT).
use-node-version=24.13.0
